<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 90">
  <!-- Browser window outline (moved up 3px) -->
  <rect x="13" y="7" width="94" height="75" rx="6" ry="6"
        fill="none" stroke="currentColor" stroke-width="4" />

  <!-- Top bar separator (also moved up 3px) -->
  <line x1="13" y1="22" x2="107" y2="22"
        stroke="currentColor" stroke-width="4" />

  <!-- Top-left circles (kept aligned with the bar) -->
  <circle cx="24" cy="15" r="3" fill="currentColor" />
  <circle cx="36" cy="15" r="3" fill="currentColor" />
  <circle cx="48" cy="15" r="3" fill="currentColor" />

  <!-- Address/search bar -->
  <rect x="80" y="11" width="20" height="6"
        rx="3" ry="3" fill="currentColor" />

  <!-- </> code symbol (unchanged) -->
  <polyline points="43,71 33,56 43,41"
            fill="none" stroke="currentColor" stroke-width="5"
            stroke-linecap="round" stroke-linejoin="round" />

  <line x1="58" y1="41" x2="68" y2="71"
        stroke="currentColor" stroke-width="5"
        stroke-linecap="round" />

  <polyline points="83,41 93,56 83,71"
            fill="none" stroke="currentColor" stroke-width="5"
            stroke-linecap="round" stroke-linejoin="round" />
</svg>
